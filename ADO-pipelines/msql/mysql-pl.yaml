trigger: none

pool:
  name: 'self-hosted-agent-pool'

resources:
  pipelines:
    - pipeline: K8s-nginx-pipeline
      source: K8s-nginx-pipeline
      trigger: true

stages:
- stage: ExecuteScript
  jobs:
  - job: ExecuteScriptJob
    steps:
    - script: /home/mnikolov/K8s-deployment-ado/ADO-pipelines/mysql/deploym.sh
      displayName: 'Execute Bash Script For MySQL'
